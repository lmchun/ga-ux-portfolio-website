import pandas as pd
## import dot environment library. put key in .env file. import that into the python. Can also look into setting it up with ur direnv setup



# df = pd.read_csv('myfile.csv')
# this is where to manage the file and then do the analysis here
